name: CI

on:
  pull_request:
    branches: [ "main", "develop" ]
  push:
    branches: [ "main", "develop" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Grant execute permission for Gradle
        run: chmod +x ./gradlew

      - name: Run unit tests
        run: ./gradlew test --no-daemon

      - name: Verify module boundaries (Spring Modulith)
        run: ./gradlew test --tests "*ModuleVerificationTest*" --no-daemon

      - name: ArchUnit gate (AU-2, AU-3)
        run: ./gradlew test --tests "*ArchitectureTest*" --no-daemon
